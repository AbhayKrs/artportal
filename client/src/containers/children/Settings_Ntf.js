import React from 'react'
import { connect } from 'react-redux'

export const Settings_Ntf = (props) => {
    return (
        <div>Settings_Ntf</div>
    )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Settings_Ntf)