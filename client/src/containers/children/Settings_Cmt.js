import React from 'react'
import { connect } from 'react-redux'

export const Settings_Cmt = (props) => {
    return (
        <div>Settings_Cmt</div>
    )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Settings_Cmt)