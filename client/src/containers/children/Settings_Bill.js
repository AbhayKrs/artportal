import React from 'react'
import { connect } from 'react-redux'

export const Settings_Bill = (props) => {
    return (
        <div>Settings_Bill</div>
    )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(Settings_Bill)